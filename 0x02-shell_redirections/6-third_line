#!/bin/bash

# Check if the file 'iacta' exists
if [ -f "iacta" ]; then
    # Use head and tail to get the third line
    line=$(head -n 3 iacta | tail -n 1)
    echo "$line"
else
    echo "File 'iacta' does not exist."
fi
